<template>
  <div class="movie-info-wrap">
    <header class="movie-info-header header">
      <h6 class="movie-header-title">Movie view</h6>
      <BIconX class="close-icon" @click="closeModal" />
    </header>
    <div class="movie-info-content">
      <BRow>
        <BCol sm="4">
          <div class="movie-poster-wrap">
            <img :src="movieInfo.Poster" class="movie-poster" />
          </div>
        </BCol>
        <BCol sm="8">
          <h3 class="movie-title">{{ movieInfo.Title }}</h3>
          <BFormRating
            class="movie-rating mb-2"
            v-model="movieInfo.imdbRating"
            readonly
            show-value
            precision="1"
            stars="10"
            show-value-max
            no-border
          />
          <p class="movie-description">{{ movieInfo.Plot }}</p>
          <div class="mt-2 mb-3">
            <BBadge variant="success" class="mr-2">{{ movieInfo.Year }}</BBadge>
            <BBadge variant="success" class="mr-2">{{
              movieInfo.Runtime
            }}</BBadge>
            <BBadge variant="success" class="mr-2">{{
              movieInfo.Genre
            }}</BBadge>
            <BBadge variant="success" class="mr-2">{{
              movieInfo.Language
            }}</BBadge>
          </div>
          <table class="table small">
            <tbody>
              <tr>
                <th>Production</th>
                <td>{{ movieInfo.Production }}</td>
              </tr>
              <tr>
                <th>Country</th>
                <td>{{ movieInfo.Country }}</td>
              </tr>
              <tr>
                <th>Director</th>
                <td>{{ movieInfo.Director }}</td>
              </tr>
              <tr>
                <th>Writer</th>
                <td>{{ movieInfo.Writer }}</td>
              </tr>
              <tr>
                <th>Actors</th>
                <td>{{ movieInfo.Actors }}</td>
              </tr>
              <tr>
                <th>Awards</th>
                <td>{{ movieInfo.Awards }}</td>
              </tr>
            </tbody>
          </table>
        </BCol>
      </BRow>
    </div>
  </div>
</template>

<script>
export default {
  name: "MovieInfoModalContent",
  props: {
    movieInfo: {
      type: Object,
      default: () => ({}),
      required: true,
    },
  },
  methods: {
    closeModal() {
      console.log("close");
    },
  },
};
</script>

<style scoped>
.movie-info-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem;
  background: linear-gradient(45deg, rgb(0, 3, 38) 0%, rgb(82, 15, 117) 100%);
  color: #fff;
}
.movie-header-title {
  margin: 0;
  font-size: 1.25rem;
  line-height: 1.5;
}
.movie-info-content {
  padding: 1rem;
  background-color: #fff;
}
.movie-poster-wrap {
  position: relative;
  padding-bottom: 150%;
  border-radius: 5px;
  overflow: hidden;
  transition: all 0.2s ease;
}
.movie-poster {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}
.movie-title {
  font-size: 3rem;
  font-weight: 300;
  line-height: 1.2;
}

.movie-rating:focus {
  box-shadow: none;
}

.movie-rating >>> .b-rating-star,
.movie-rating >>> .b-rating-value {
  justify-content: flex-start;
  flex-grow: 0 !important;
  font-size: 1.2rem;
  font-weight: 300;
  padding: 0;
}

.movie-rating {
  padding: 0 !important;
}

.movie-rating >>> .b-rating-star + .b-rating-star {
  margin-left: 4px;
}
.movie-rating >>> .b-rating-value {
  margin-left: 10px;
}

.movie-rating >>> .b-rating-star .b-rating-icon {
  color: #ffc107;
}

.movie-description {
  font-size: 1.1rem;
  font-weight: 300;
}
</style>
